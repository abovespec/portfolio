# RecipeScale

Recipe scaler, nutrition calculator by ingredient, meal plan generator by calorie target, and leftover ingredient finder.

Generated from `packages/site-template` via `scripts/create-site.mjs`.

Local dev:

    pnpm install
    pnpm dev

Deploy: push to `main` (requires Cloudflare secrets on the GitHub repo — see the template README).
