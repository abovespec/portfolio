---
/**
 * PairPage — pair converter page shell.
 *
 * Renders the pair's MDX content slot.
 * The live calculator / rate display is injected by PairLayout.
 */
---
<div class="mt-8 prose wide">
  <slot />
</div>