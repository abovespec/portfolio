# Cloudflare Pages project configuration.
# Replace `__PROJECT_NAME__` with the Cloudflare Pages project slug for this
# site. One project per site; the account is shared across the network.
name = "__PROJECT_NAME__"
pages_build_output_dir = "dist"
compatibility_date = "2025-06-01"
